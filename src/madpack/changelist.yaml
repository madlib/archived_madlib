# Modules added in the current version
# --- example --
#   validation:
#   lda:
#   elastic_net:
#   summary:
new module:

# Changes in the types (UDT) defined in MADlib
#   - c: changes in existing UDT
#   - i: newly added UDT
# --- example ---
#   __utils_scaled_data: i
#   __utils_scales: i
#   logregr_result: c
#   logregr_result: c
udt:

# Functions (UDF) removed from the previous version
# Overloaded functions should be separately mentioned
# --- example ---
#   - compute_logregr:
#           rettype: integer
#           argument: source character varying, depColumn character varying, indepColumn character varying, maxNumIterations integer, optimizer character varying, "precision" double precision
udf:

# Aggregates (UDA) removed from the previous version
# Overloaded functions should be mentioned separately
# --- example ---
# - logregr_irls_step:
#      rettype: double precision[]
#      argument: boolean, double precision[], double precision[]
uda:

# Cast operators (UDC) changed in the current version
# --- example ---
#   bool2text:
#        sourcetype: boolean
#        targettype: text
udc:
