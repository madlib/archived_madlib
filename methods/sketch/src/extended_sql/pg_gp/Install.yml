# required flags include:
#  - "module": the name of the module 
#  - "bw" and "fw": the backward- and forward-rolling migration scripts
module: sketch
bw: sketches_drop.sql
fw: sketches.sql
# the optional depends key allows you to specify dependencies on a list of 
# other methods.  sketch has no such dependencies: 
# depends: [method1, method2]
# see methods/profile/src/extended_sql/pg_gp/Install.yml for a real example.