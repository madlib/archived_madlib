Code donated by Daisy Wang, daisy@eecs.berkeley.edu, under the same BSD 
License as MADlib.

This code has not been tested by the MADlib team.  Due to the use of 
WITH RECURSIVE, it currently requires PostgreSQL >= 8.4, and will not work 
with Greenplum. The intention is to refactor it to avoid the use of 
WITH RECURSIVE.
