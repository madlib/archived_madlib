// Change the following to the root of the PostgreSQL/Greenplum installation
// (which is "/usr/local/greenplum-db" by default)
DB_HOME = /Library/UNIX/postgresql

// Change the following to the include-directory (relative to $DB_HOME)
DB_INCLUDE = include
